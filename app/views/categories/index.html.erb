<ul>
  <% @categories.each do |category| %>
    <li>
      <%= link_to category.name, category_path(category) %>
      <%= render partial: "items/ol_items", locals: {items: category.items} %>
    </li>
  <% end %>
</ul>
