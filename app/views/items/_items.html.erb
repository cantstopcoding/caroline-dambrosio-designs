<ul>
    <% items.each do |item| %>
        <li><%= link_to item.name, item_path(item) %> - <%= item.comments.size %> comments</li>
        <% if @display_user %>
            By: <%= link_to item.user.username, user_path(item.user) %> 
        <% end %>
    <% end %>
</ul>